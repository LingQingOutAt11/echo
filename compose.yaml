services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: heikesong
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - heikesong-postgres:/var/lib/postgresql/data

volumes:
  heikesong-postgres:
